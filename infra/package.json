{
  "name": "cdk-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure app",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.200.0",
    "constructs": "^10.4.2"
  }
}
